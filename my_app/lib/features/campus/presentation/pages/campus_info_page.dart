import 'package:flutter/material.dart';

class CampusInfoPage extends StatelessWidget {
  const CampusInfoPage({super.key});
  @override
  Widget build(BuildContext context) {
    return const Scaffold(
      body: Center(child: Text('Campus Info')),
    );
  }
}
